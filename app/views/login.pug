extends layout

block content
  form#submitSignup(action="/users/login" method="POST")
    label(for="email") 
      | Email
    input(type="text" id="email" name="email" required)
    br
    label(for="password")
      | Password
    input(type="password" id="password" name="password" autocomplete="new-password" required)
